language: node_js
node_js: lts/*

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm test

after_success:
  - git remote add deploy $SERVER_USERNAME:$SERVER_IP
  - git push deploy master
branches:
  - master
